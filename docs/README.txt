GRIDIRON GANG PLAYTEST BUILD

Serve this folder over HTTPS or publish it with GitHub Pages. Open index.html
through the hosted URL; browser security prevents launching the game reliably
by double-clicking the local file. Saves remain local to each browser and site.
